{
  "name": "orbtasoft-automation-android",
  "version": "1.0.0",
  "description": "Orbtasoft Automation — Android (Capacitor)",
  "scripts": {
    "sync":  "node sync-www.js && npx cap sync android",
    "open":  "npx cap open android",
    "build": "node sync-www.js && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core":    "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
